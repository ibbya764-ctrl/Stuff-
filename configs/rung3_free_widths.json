{
  "name": "rung3_free_widths",
  "preregistration": {
    "prediction": "Control arm for rung3_critical_line; identical except width_mode = free. (Parameter-count delta is d_state-1 scalars per layer, ~0.01% — within the 2% matched-budget tolerance.)",
    "kill_condition": "n/a (control)."
  },
  "seed": 0,
  "model": {"d_model": 128, "n_layers": 4, "d_state": 64, "width_mode": "free", "freq_init": "s4"},
  "task": {"name": "copy", "train_length": 64, "eval_lengths": [64, 128, 256]},
  "train": {"steps": 4000, "batch_size": 32}
}
